# requirements.txt
# Python dependencies for the TaskFlow backend.
# Install with: pip install -r requirements.txt

# Core
fastapi==0.107.0
uvicorn==0.23.2

# Database
SQLAlchemy==2.0.25
alembic==1.11.1
sqlite3-binary==0.0.1   # only if you want SQLite prebuilt

# Pydantic
pydantic<2.0
pydantic-settings<2.0

# Utilities
python-multipart==0.0.6
requests==2.32.0
PyYAML==6.0

# Build tools (prevent wheel issues)
setuptools>=70.1
wheel