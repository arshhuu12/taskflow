# requirements.txt
# Python dependencies for the TaskFlow backend.
# Install with: pip install -r requirements.txt

# --- Web Framework ---
fastapi==0.111.0          # Modern async web framework
uvicorn[standard]==0.29.0 # ASGI server (includes watchfiles for --reload)

# --- Database ---
sqlalchemy==2.0.30        # ORM and query builder
alembic==1.13.1           # Database migrations
psycopg2-binary==2.9.9    # PostgreSQL driver

# --- Validation & Settings ---
pydantic==2.7.1           # Data validation (Pydantic v2)
pydantic-settings==2.2.1  # BaseSettings for env-var loading

# --- Utilities ---
python-dotenv==1.0.1      # Load .env file into environment

# --- Dev / Testing ---
httpx==0.27.0             # Async HTTP client (useful for TestClient)
pytest==8.2.0
pytest-asyncio==0.23.6
